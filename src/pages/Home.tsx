/* The home route delegates its full viewport to the animated security-focused hero. */
import AetherGalaxyHero from '../components/ui/AetherGalaxyHero';

export default function Home() {
  return <AetherGalaxyHero />;
};
